{
  "name": "spenditure",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm dev:server\" \"pnpm dev:web\"",
    "dev:server": "pnpm --filter @spenditure/server dev",
    "dev:web": "pnpm --filter @spenditure/web dev",
    "build": "pnpm --filter @spenditure/shared build && pnpm --filter @spenditure/web build",
    "db:init": "pnpm --filter @spenditure/server db:init",
    "db:seed": "pnpm --filter @spenditure/server db:seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.8.0"
  },
  "packageManager": "pnpm@10.30.3",
  "engines": {
    "node": ">=20.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "better-sqlite3"]
  }
}
